/** @type {import('next').NextConfig} */
const nextConfig = {
	images: {
		domains: [
			"images.unsplash.com",
			"res.cloudinary.com",
			"ui-avatars.com",
			"img.clerk.com"
		],
	},
}

module.exports = nextConfig