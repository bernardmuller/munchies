import { IPrimaryLayout } from './PrimaryLayout';

const PrimaryLayout: IPrimaryLayout = {
  children: 'PrimaryLayout Container works!!',
};

export const mockPrimaryLayoutProps = {
  PrimaryLayout,
};
