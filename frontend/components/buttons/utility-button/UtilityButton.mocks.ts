import { IUtilityButton } from './UtilityButton';

const utility: IUtilityButton = {
  variant: 'save',
  onClick: () => {},
  border: false,
};

export const mockUtilityButtonProps = {
  utility,
};
