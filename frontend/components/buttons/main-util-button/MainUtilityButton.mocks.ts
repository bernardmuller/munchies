import { IMainUtilityButton } from './MainUtilityButton';

const utility: IMainUtilityButton = {
  variant: 'save',
  onClick: () => {},
  border: false,
};

export const mockMainUtilityButtonProps = {
  utility,
};
